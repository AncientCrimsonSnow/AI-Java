package Utilities;

public class PlayTreeBuilder {
    /*
    public static void main(String[] args) {
        var hashTable = new ArrayList<byte[]>();
        var graph = new DirectedGraph();

        var p0Boards = new ArrayList<Board>();
        p0Boards.add(new Board(PlayerNumber.p0));

        while (!p0Boards.isEmpty()){
            var p0Baords =
            var p1Boards = new ArrayList<Board>();
            var p2Boards = new ArrayList<Board>();
            for(var p0Board : p0Boards){
                var validMoves = p0Board.GetValidMoves();
                for(var validMove : validMoves){
                    var p1Board = new Board(PlayerNumber.p1, p0Board.GetBoard());
                    p1Board.UpdateBoard(validMove);
                    p1Boards.add(p1Board);
                }
            }
            p0Boards.clear();

            for(var p1Board : p1Boards){
                var validMoves = p1Board.GetValidMoves();
                for(var validMove : validMoves){
                    var p2Board = new Board(PlayerNumber.p2, p1Board.GetBoard());
                    p2Board.UpdateBoard(validMove);
                    p2Boards.add(p2Board);
                }
            }
            for(var p2Board : p2Boards){
                var validMoves = p2Board.GetValidMoves();
                for(var validMove : validMoves){
                    var p0Board = new Board(PlayerNumber.p2, p2Board.GetBoard());
                    p0Board.UpdateBoard(validMove);
                    p0Boards.add(p0Board);
                }
            }
        }
    }
    */
}
